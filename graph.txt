tampilkan commit:
git log --all --decorate --oneline --graph

pindah antar commit:
git checkout (kodecommit)

untuk simpan:
git add .
git commit -am "komentar"

cek status:
git status

mengembalikan file yang belum di commit;
git stash